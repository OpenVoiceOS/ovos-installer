---
- name: Reload Systemd User
  become: true
  become_user: "{{ ovos_installer_user }}"
  ansible.builtin.systemd_service:
    daemon_reload: true
    scope: user

- name: Run Depmod
  ansible.builtin.command:
    cmd: depmod -a 6.6.22-v8+
  changed_when: false
